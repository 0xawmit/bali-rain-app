# Bali Rain Rewards App - Environment Variables
# Copy this file to .env.local and fill in your actual values

# ============================================================================
# SUPABASE CONFIGURATION
# ============================================================================

# Your Supabase project URL
# Get this from: Supabase Dashboard → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anonymous (public) key
# This is safe to expose to the browser
# Get this from: Supabase Dashboard → Settings → API → Project API keys → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# EDGE FUNCTION ENDPOINT
# ============================================================================

# Your deployed QR scan Edge Function URL
# Get this after deploying: Supabase Dashboard → Edge Functions → qr-scan → Copy URL
# Format: https://your-project-ref.supabase.co/functions/v1/qr-scan
NEXT_PUBLIC_QR_SCAN_ENDPOINT=https://your-project-ref.supabase.co/functions/v1/qr-scan

# ============================================================================
# SERVER-ONLY VARIABLES (Optional)
# ============================================================================

# Supabase service role key (ONLY for scripts - never expose to client!)
# Get this from: Supabase Dashboard → Settings → API → Project API keys → service_role
# Used in: scripts/generate-qr-codes.ts, Edge Functions
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================================
# NOTES
# ============================================================================

# NEXT_PUBLIC_ prefix: Exposed to the browser (client-side)
# No prefix: Server-side only, never exposed to client

# Don't commit .env.local to Git! Add to .gitignore.

# For Vercel deployment:
# 1. Add all NEXT_PUBLIC_ variables in Vercel Dashboard → Settings → Environment Variables
# 2. Set for Production, Preview, and Development environments
# 3. Redeploy after adding variables

# For local development:
# 1. Copy this file to .env.local (cp env.example .env.local)
# 2. Fill in your Supabase credentials
# 3. Run npm run check:env to verify

# For Edge Function deployment:
# The SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are automatically
# provided by Supabase to Edge Functions - you don't need to set them manually.
